<!-- <div class="row">
  <aside class="col-md-4">
    <section class="user_info">
      <%= render 'shared/user_info' %>
    </section>
    <section class="story_form">
      <%= render 'shared/story_form' %>
    </section>
  </aside>
</div> -->

<section id="blog">
  <span class="title"><%= @story.title %></span>
  <span class="content"><%= @story.content %></span>
</section> 