<!-- Header -->
<%= render 'header' %>

<!-- Flash Section -->
<% if !flash.empty? %>
    <%= render 'shared/flash' %>
<% end %>

<!-- Intro -->
<%= render 'intro' %>

<!-- Parallax 1 -->
<%= render 'parallax1' %>

<!-- Blog -->
<%= render 'blog' %>

<!-- Parallax 2 -->
<%= render 'parallax2' %>

<!-- Pictures -->
<%= render 'pictures' %>

<!-- Videos -->
<%= render 'videos' %>

<!-- Footer -->
<%= render 'footer' %>




