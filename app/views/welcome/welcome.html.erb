<!-- Header -->
<%= render 'header' %>

<!-- Flash Section -->
<% if !flash.empty? %>
    <%= render 'shared/flash' %>
<% end %>

<!-- Intro -->
<%= render 'intro' %>

<!-- Parallax 1 -->
<%= render 'parallax1' %>

<!-- Blog -->
<%= render 'blog' %>

<!-- Parallax 2 -->
<%= render 'parallax2' %>

<!-- Pictures -->
<%= render 'pictures' %>

<!-- Parallax 3 -->
<%= render 'parallax3' %>

<!-- Recordings -->
<%= render 'recordings' %>

<!-- Obituary -->
<%= render 'obit' %>

<!-- Guest Book -->
<%= render 'guestbook' %>

<!-- Eventually we want to render 'videos' here Videos -->

<!-- Footer -->
<%= render 'footer' %>




